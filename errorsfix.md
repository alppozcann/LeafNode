# errorsfix.md

| File | Line(s) | Severity | Description | Fix |
|------|---------|----------|-------------|-----|
| `leafnode-backend/app/routers/commands.py` | 51–52 | **Critical** | `datetime` and `timezone` used but never imported → `NameError` on every successful MQTT command publish, silently leaving the command stuck in `PENDING` status | Added `from datetime import datetime, timezone` |
| `leafnode-backend/app/database.py` | 6 | **High** | No `pool_pre_ping=True` — stale DB connections after a network drop or DB restart are not recycled; the next query fails with a connection error rather than transparently re-connecting | Added `pool_pre_ping=True`, `pool_size=5`, `max_overflow=10` to `create_async_engine` |
| `leafnode-backend/app/influx_listener.py` | 24–48 | **High** | Single `AsyncSession` shared across all readings in a batch — if `run_anomaly_detection` raises mid-transaction (e.g. a DB constraint error), the session enters an aborted state and all subsequent readings in the same batch fail silently | Moved each reading into its own `async with AsyncSessionLocal() as db:` block; errors on one reading no longer affect others |
| `leafnode-backend/app/influx_listener.py` | 49 | **High** | `last_check_time` only advanced inside the shared `try` block — if the InfluxDB query succeeded but the DB write later failed, `last_check_time` was not advanced, causing the same InfluxDB window to be re-fetched and written as duplicate `SensorReading` records on the next successful cycle | Separated InfluxDB fetch into its own `try/except`; `last_check_time = now` is set immediately after a successful fetch, before any DB writes |
| `leafnode-frontend/src/api.js` | 3–12 | **High** | No fetch timeout — a slow or hung backend response blocks all API calls indefinitely, freezing the polling loop | Added `AbortController` with a 15-second timeout to every `fetch` call |
| `leafnode-frontend/src/api.js` | 3–12 | **High** | No retry logic — a single transient network failure is immediately surfaced as a UI error | Added 3-attempt retry with exponential backoff (500ms → 1s → 2s) on `AbortError` and `TypeError` (network-level failures only; HTTP errors are not retried) |
| `leafnode-backend/app/services/llm_explanation.py` | 119 | **Medium** | No timeout on `client.aio.models.generate_content` — a slow or hung Gemini response blocks the entire anomaly detection pipeline indefinitely for that reading | Wrapped call with `asyncio.wait_for(timeout=30.0)`; timeout is caught by the existing fallback loop which moves to the next model |
| `leafnode-backend/app/services/llm_threshold.py` | 29 | **Medium** | No timeout on `client.aio.models.generate_content` — a hung Gemini response blocks the `/plants` POST endpoint (and the requesting user) indefinitely | Wrapped call with `asyncio.wait_for(timeout=20.0)`; `TimeoutError` is converted to `ThresholdGenerationError` and returns HTTP 502 |
| `leafnode-frontend/src/App.jsx` | 76–81 | **Medium** | No offline detection — polling continues when the browser has no network, producing a stream of `TypeError: Failed to fetch` errors surfaced as UI error banners every 30 seconds | Added `navigator.onLine` initial state, `window.online`/`offline` event listeners, offline banner in the header, and guards that skip `fetchAll` and `setInterval` ticks when offline; polling resumes automatically on reconnect |
| `leafnode-backend/app/services/mqtt_listener.py` | 131–138 | **Low** | Fixed 5-second reconnect delay on every MQTT failure — repeated rapid broker outages produce a reconnect storm with no back-off | Added exponential backoff starting at 1s, doubling on each failure up to a 60s cap; reset to 1s on a successful connection |
